server {
    #add_header Access-Control-Allow-Origin *;
    listen 8087;
    location / {
        root    /usr/share/nginx/html;
        index   index.html;
        try_files $uri $uri/ /index.html;
    }
}